program teste;

var foo, bar, dog: real;
var rons: boolean;

! testando comentario no @codigo <> demais

begin
    foo := 10;
    bar := 222;

    ! se for maior que 0
    while rod > 0 do
        begin
            rons := false;
        end;

    if (foo > bar or dog) then
        bar := foo + 10 * 5;
end.